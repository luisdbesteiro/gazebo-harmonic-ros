# Resultados

Guarda aqui solo resultados curados y reproducibles de la simulacion:

- resumenes `.md`
- tablas `.csv`
- graficas ligeras
- configuraciones exactas de ejecucion

No subas aqui bolsas ROS, bases SQLite, videos, logs completos ni datos con rutas locales, tokens o URLs firmadas. Para datos brutos usa `workspace/results/raw/`, que esta excluido de Git.
